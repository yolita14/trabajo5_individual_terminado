<%= form_with(model: etiquetum) do |form| %>
  <% if etiquetum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(etiquetum.errors.count, "error") %> prohibited this etiquetum from being saved:</h2>

      <ul>
        <% etiquetum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :publicacion_ids, style: "display: block" %>
    <%= form.collection_check_boxes :publicacion_ids, @publicacions, :id, :titulo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
